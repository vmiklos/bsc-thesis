includes.tex := abstract.tex preliminaries.tex

%.eps: %.pic
	$(QUIET)$(call echo-graphic,$^,$@)
	$(QUIET)pic2plot -Tps $^ >$@

%.eps: %.png
	$(QUIET)$(call echo-graphic,$^,$@)
	$(QUIET)convert $^ $@

# vim: ft=make
